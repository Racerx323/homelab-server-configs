# Failed resource acceptance archive

All 48 consumed input hashes in `bundle.json` were verified and preserved
in `objects/`. `operation.yaml` is the consumed definition; the sanitized result
is `../../manifests/application-startup-result.json`. Raw evidence remains private.

Tag: `nautobot-application-startup-v5-failed`; publication pending.
