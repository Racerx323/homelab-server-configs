# Accepted application startup archive

All 48 consumed input hashes in `bundle.json` were verified and preserved
in `objects/`. `operation.yaml` is the consumed definition. Sanitized results
are in `../../manifests/application-startup-result.json`; raw evidence stays private.

All seven startup groups passed. Services remain running. Workload, persistence,
Caddy and application recovery acceptance remain separate.

Tag: `nautobot-application-startup-v6-accepted`; publication pending.
