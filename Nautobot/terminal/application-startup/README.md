# Consumed startup inputs

`bundle.json` maps original input names to SHA-256 object files under `objects/`.
All original rows were verified before archival. These are the consumed inputs,
not the corrected reusable code elsewhere in this commit. `operation.yaml` is
the original definition. The diagnostic bundle preserves the separately approved
configuration check. Raw output and credentials remain outside Git. The sanitized
result is `../../manifests/application-startup-result.json`.
