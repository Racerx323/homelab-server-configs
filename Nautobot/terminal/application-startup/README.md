# Failed application startup archive

Exact nonsecret consumed inputs are stored by SHA-256 in `objects/`;
`bundle.json` maps each original input to its content. All 48 input hashes
were verified. The consumed definition is `operation.yaml`. Sanitized outcome:
`../../manifests/application-startup-result.json`. Raw logs remain private.

Archive tag: `nautobot-application-startup-v3-failed`.
Publication is pending; the peeled tag identifies the archive commit.
