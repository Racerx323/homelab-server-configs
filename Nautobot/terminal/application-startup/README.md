# Consumed startup bundle

Exact consumed inputs for the failed application startup are retained by SHA-256 in objects/. bundle.json maps their original names. Runtime acceptance failed; cleanup stopped all services, preserving data. Raw logs and credential values are excluded.
