# Failed startup session acceptance archive

All 48 consumed input hashes in `bundle.json` were verified against the exact
executed bundle and retained in `objects/`. `operation.yaml` is the consumed
definition; `../../manifests/application-startup-result.json` records the sanitized
outcome. Raw evidence remains private.

Tag: `nautobot-application-startup-v4-failed`; publication pending.
