# Accepted synthetic workload archive

Accepted only the repeat-fixture synthetic pilot workload. All five phases,
15 Jobs, deterministic exports, ownership continuity, actual backup overlap,
full integrity check and resource/storage observation passed. Full stage 5,
real-inventory representativeness, persistence and application restore remain open.

The node finished successfully; the controller was interrupted for an unproven
reason. Node-local evidence and independent readback support this decision.
Scoped recovery removed exact temporary module bytes and verified absence.
Controller credential directories are absent now; historical finalizer execution
is unproven after workstation restart and loss of temporary evidence. No seamless
controller completion or interruption-resilience qualification is claimed.

Exact consumed inputs are in bundle/ and source/. Source inputs match published
preparation 127b0ff. The historical operation source_commit names its predecessor.
result.json records sanitized decisions and private evidence hashes; raw evidence
is excluded. Annotated tag `nautobot-workload-qualification-v2-accepted` identifies
this archive commit. Publication-pending fields are historical once published.

The recovery playbook copy has normalized YAML indentation with unchanged parsed
content. Its original execution bytes remain privately hash-bound in result.json.
