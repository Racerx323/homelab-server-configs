# Intended-state record; do not include this file directly from lighttpd.
# prepare-lighttpd-config.sh applies these values to a staged configuration tree.
server.bind = "127.0.0.1"
server.port = 8080
server.errorlog-use-syslog = "enable"
accesslog.use-syslog = "enable"
ssl.engine = "disable"
