---
schema: pihole-v5-config-action29b-v1
action: 29b
mode: definition-only
transactional: true
standby_first: true
consumed_failure:
  action: 29a
  outer_sha256: 18aea5e77db8d8deccefa200379553ec0cb9aab80ed283d91b6ab9d2ba50bed4
  evidence: /tmp/caddy-ssh-evidence/action29a/run.4V1UBR
  classification: exact repository and live Pi-hole v5 artifact mismatch before Node B contact
accepted_baseline:
  action: 28ah
  ownership: Node A MASTER with four shared VIPs; Node B BACKUP with zero shared VIPs
targets:
  ftl:
    repository_path: /home/aaron/code/homelab-dns/Pi-Hole/configs/pihole-FTL.conf
    live_path: /etc/pihole/pihole-FTL.conf
    sha256: a1dc88b1f696a6870e38025be113ff8664750fca6265c79a2aee12f80898cfa3
    owner: pihole
    group: root
    mode: '0664'
  dnsmasq:
    repository_path: /home/aaron/code/homelab-dns/Pi-Hole/configs/local.theama.co.conf
    live_path: /etc/dnsmasq.d/local.theama.co.conf
    sha256: 39fa219a7d1c81c7fb36d89bc17ba1caae26f3472eb861a750a8ba03ae55b026
    owner: root
    group: root
    mode: '0644'
sequence:
  - validate both exact repository identities locally
  - install and validate Node B under a protected backup boundary
  - run pihole-FTL --test before restarting Node B DNS
  - independently accept Node B service and DNS continuity
  - independently reconfirm Node A service and DNS continuity
  - install and validate Node A under a protected backup boundary
  - run pihole-FTL --test before restarting Node A DNS
  - independently accept both exact installations and dual-node continuity
rollback:
  node_local: restore exact manifest-pinned backup and restart only local Pi-hole DNS
  cross_node_order: Node A then Node B
  manual_intervention_status: 125
acceptance:
  - both nodes run Pi-hole and FTL v5
  - pihole-FTL, Unbound, Keepalived, and Caddy remain active
  - Node A remains IPv4 and IPv6 MASTER with all four shared VIPs
  - Node B remains IPv4 and IPv6 BACKUP with zero shared VIPs
  - A, AAAA, PTR, and SRV local-zone answers remain exact
  - both exact target hashes and protected transaction markers exist
  - every SSH stdout, stderr, and status is retained beneath local /tmp evidence
validation:
  focused_host: required
  focused_debian_12: required
  complete_historical_suite: prohibited
authorization:
  definition_requires_separate_authorization: false
  execution_requires_exact_outer_sha256: true
execution_status: not_executed
