# Future application reverse-proxy template input.
# This file is not installed or consumed by current production tooling.
#
# A future documentation task will add explicit placeholders, TLS policy,
# trusted health validation, default-deny behavior, and a validation example.

https://app.example.internal {
    reverse_proxy 127.0.0.1:8080
}
